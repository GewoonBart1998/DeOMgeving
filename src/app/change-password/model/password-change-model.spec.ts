import { ChangePasswordModel } from './password-change-model';

describe('ChangePasswordModel', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordModel()).toBeTruthy();
  });
});
